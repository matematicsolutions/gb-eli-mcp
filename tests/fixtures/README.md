# Fixtures

This connector's smoke tests hit the live legislation.gov.uk site directly (same pattern
as every sibling connector's `test_smoke.py`), so no offline XML fixtures are required for
the current test suite. This directory is reserved for future snapshot fixtures - all
public legislation.gov.uk data, published under the Open Government Licence v3.0.
